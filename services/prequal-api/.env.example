# Database Configuration
DATABASE_URL=postgresql://loan_user:loan_password@localhost:5432/loan_prequalification

# Encryption Key (generate using: python scripts/generate_encryption_key.py)
ENCRYPTION_KEY=your-base64-encoded-32-byte-key-here

# Service Configuration
SERVICE_NAME=prequal-api
LOG_LEVEL=INFO

# Kafka Configuration (for future use)
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
